#include<stdio.h>
#include<math.h>

int main()
{
    int a;
    printf("Enter the value of a");
    scanf("%d",&a);
float root = sqrt(a);
    printf("The square root of %d is %f",a,root);
    
    return 0;
}