#include<stdio.h>
int main()
{
int side ;
printf("enter the side of the square");
scanf("%d",& side);
    int area ;
    area = side * side;
    printf("the area of square is %d",area);
    return 0;
}